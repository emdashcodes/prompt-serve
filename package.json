{
  "name": "mcp-prompt-server",
  "version": "1.0.0",
  "description": "MCP server for handling prompts",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc && node -e \"require('fs').chmodSync('dist/index.js', '755')\"",
    "start": "node dist/index.js",
    "dev": "tsc -w",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "gray-matter": "^4.0.3",
    "typescript": "^4.9.3",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^22.13.10"
  }
}
